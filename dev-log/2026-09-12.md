## Log: 2026-09-12

### What I Did:
* Setup tanstack persit query hook
* Learned how to use expo-file system for cache
* Added functions `getJSON` and `setJson` to handle json being storaged, with safety guardrails and early returns.
* Asked AI for help to understand how to use  `react-native-mmkv` v4.
* Ask AI to generate stroage driver that uses `asyncStorage`, `MMKV` and web's `localstorage`  based on what is needed.


### CHALLENAGES/BUGS

* I might have to make two tanstack porviders. One for light caching, and  the other for data that will not be deleted like questions.

* web doesn't use async storage or MMKV so localstorage fallback was provided
* Expo go can't use MMKV so  I had to fallback to asyncStorage



